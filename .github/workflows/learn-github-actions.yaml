name: learn-github-actions
run-name: ${{ gitub.actor }} is learning github learn-github-actions
on: [push]

jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@4
      - uses: actions/setup-node@4
        with:
          node-version: '20'
      - run: npm install -g bats
      - run: bats -v